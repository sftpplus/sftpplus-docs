* `{now.cwa_14051}` - Human-readable local date and time
* `{now.iso_8601}` - ISO 8601 date and time
* `{now.iso_8601_fractional}` - ISO 8601 date and time with fractional seconds
* `{now.iso_8601_local}` - ISO 8601 date and time in the local time zone
* `{now.iso_8601_basic}` - Basic ISO 8601 date and time
* `{now.iso_8601_compact}` - ISO 8601 compatible with Windows file names
* `{now.timestamp}` - Unix timestamp with decimals
* `{now.year_decimal}` - Year as a decimal number, such as `2021`
* `{now.year_no_century}` - Zero-padded year without century, such as `21`
* `{now.month_decimal}` - Month as a decimal number, such as `1`
* `{now.month_padded}` - Zero-padded month, such as `01`
* `{now.day_padded}` - Zero-padded day of the month, such as `07`
* `{now.day_decimal}` - Day of the month as a decimal number, such as `7`
* `{now.day_of_year_padded}` - Zero-padded day of the year, such as `007`
* `{now.hour_padded_24}` - Zero-padded hour on a 24-hour clock
* `{now.hour_decimal_24}` - Hour on a 24-hour clock as a decimal number
* `{now.hour_padded_12}` - Zero-padded hour on a 12-hour clock
* `{now.hour_decimal_12}` - Hour on a 12-hour clock as a decimal number
* `{now.hour_am_pm}` - Locale equivalent of `AM` or `PM`
* `{now.minute_padded}` - Zero-padded minute
* `{now.minute_decimal}` - Minute as a decimal number
* `{now.second_padded}` - Zero-padded second
* `{now.second_decimal}` - Second as a decimal number
